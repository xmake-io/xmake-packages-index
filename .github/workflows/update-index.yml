name: Update index & deploy site

on:
  schedule:
    # Hourly at minute 0. GitHub may delay scheduled runs under load.
    - cron: '0 * * * *'
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout site repo
        uses: actions/checkout@v4

      # Provision xmake via the official action instead of having build.sh
      # bootstrap it — keeps the CI step explicit and lets us pin/branch
      # easily without touching the script.
      - uses: xmake-io/github-action-setup-xmake@v1
        with:
          xmake-version: latest

      # node, npm, git, curl, python3 are preinstalled on ubuntu-latest.
      # Cache npm + the xmake-repo checkout so hourly reruns finish quickly.
      - name: Cache npm + xmake-repo checkout
        uses: actions/cache@v4
        with:
          path: |
            web/node_modules
            .cache/xmake-repo
          key: ${{ runner.os }}-pkgs-index-${{ hashFiles('web/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-pkgs-index-

      - name: Build dataset + static site
        env:
          # gh-pages serves at https://<user>.github.io/<repo>/ unless a CNAME
          # is present. The default below targets the project sub-path; override
          # at the repo level by setting VITE_BASE in Variables (e.g. "/").
          VITE_BASE: ${{ vars.VITE_BASE || '/xmake-packages-index/' }}
        run: ./build.sh

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
