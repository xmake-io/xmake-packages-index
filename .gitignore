# dependencies
node_modules/
web/node_modules/
# Stub root package.json has no real deps; ignore its lockfile so it doesn't
# get committed accidentally. (web/package-lock.json is intentionally tracked.)
/package-lock.json

# build outputs
web/dist/
web/public/data/

# indexer working dirs
.cache/
dist/

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# logs
npm-debug.log*
yarn-error.log
